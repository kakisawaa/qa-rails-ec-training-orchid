# Windows用のdocker-compose.ymlです
# 新規にdocker-compose.ymlを作成してください
# その上でこの内容をdocker-compose.ymlにペーストしてください

# version: "3.9"
# services:
#   db:
#     image: postgres
#     volumes:
#       - dbdata:/var/lib/postgresql/data #Windowsの場合
#     environment:
#       POSTGRES_PASSWORD: password
#   web:
#     build: .
#     command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
#     volumes:
#       - .:/myapp
#     ports:
#       - "3000:3000"
#     depends_on:
#       - db

# volumes:
#   dbdata:
